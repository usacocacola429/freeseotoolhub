<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Hreflang Tag Generator - International SEO Tool | 100+ Languages</title>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
<style>
* {
margin: 0;
padding: 0;
box-sizing: border-box;
font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}

body {
background: linear-gradient(145deg, #0B1120 0%, #1A2332 100%);
min-height: 100vh;
padding: 30px;
color: #fff;
}

.container {
max-width: 1400px;
margin: 0 auto;
}

/* Header */
.header {
text-align: center;
margin-bottom: 40px;
position: relative;
}

.header h1 {
font-size: 3.2rem;
background: linear-gradient(135deg, #60A5FA, #34D399, #FBBF24);
-webkit-background-clip: text;
-webkit-text-fill-color: transparent;
margin-bottom: 15px;
display: flex;
align-items: center;
justify-content: center;
gap: 20px;
}

.header h1 i {
background: linear-gradient(135deg, #60A5FA, #34D399);
-webkit-background-clip: text;
-webkit-text-fill-color: transparent;
font-size: 3rem;
}

.header p {
color: #94A3B8;
font-size: 1.2rem;
max-width: 700px;
margin: 0 auto;
}

.language-badge {
display: flex;
gap: 10px;
justify-content: center;
margin-top: 20px;
flex-wrap: wrap;
}

.badge {
background: rgba(96, 165, 250, 0.1);
border: 1px solid rgba(96, 165, 250, 0.2);
color: #60A5FA;
padding: 8px 20px;
border-radius: 40px;
font-size: 0.95rem;
display: flex;
align-items: center;
gap: 8px;
}

.badge i {
font-size: 1rem;
}

/* Main Card */
.main-card {
background: rgba(255, 255, 255, 0.02);
backdrop-filter: blur(10px);
border: 1px solid rgba(255, 255, 255, 0.05);
border-radius: 32px;
padding: 40px;
margin-bottom: 30px;
box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
}

/* URL Input Section */
.url-section {
background: rgba(255, 255, 255, 0.02);
border: 1px solid rgba(255, 255, 255, 0.05);
border-radius: 24px;
padding: 30px;
margin-bottom: 30px;
}

.section-title {
display: flex;
align-items: center;
gap: 12px;
margin-bottom: 25px;
color: #FBBF24;
font-size: 1.3rem;
}

.section-title i {
font-size: 1.6rem;
}

.url-input-group {
display: flex;
gap: 15px;
margin-bottom: 20px;
flex-wrap: wrap;
}

.url-input-wrapper {
flex: 3;
position: relative;
min-width: 300px;
}

.url-input-wrapper i {
position: absolute;
left: 20px;
top: 50%;
transform: translateY(-50%);
color: #60A5FA;
font-size: 1.2rem;
}

.url-input-wrapper input {
width: 100%;
padding: 18px 25px 18px 55px;
background: rgba(255, 255, 255, 0.02);
border: 2px solid rgba(255, 255, 255, 0.05);
border-radius: 16px;
color: white;
font-size: 1rem;
transition: all 0.3s ease;
}

.url-input-wrapper input:focus {
outline: none;
border-color: #60A5FA;
background: rgba(255, 255, 255, 0.03);
box-shadow: 0 0 0 4px rgba(96, 165, 250, 0.1);
}

.url-input-wrapper input::placeholder {
color: #475569;
}

.add-btn {
flex: 0.5;
min-width: 120px;
background: rgba(96, 165, 250, 0.1);
border: 1px solid rgba(96, 165, 250, 0.2);
color: #60A5FA;
border-radius: 16px;
font-size: 1rem;
font-weight: 600;
cursor: pointer;
transition: all 0.3s ease;
display: flex;
align-items: center;
justify-content: center;
gap: 8px;
}

.add-btn:hover {
background: #60A5FA;
color: white;
transform: translateY(-2px);
}

/* URLs List */
.urls-list {
margin-top: 20px;
}

.url-item {
background: rgba(255, 255, 255, 0.02);
border: 1px solid rgba(255, 255, 255, 0.05);
border-radius: 12px;
padding: 15px 20px;
margin-bottom: 10px;
display: flex;
align-items: center;
justify-content: space-between;
flex-wrap: wrap;
gap: 15px;
animation: slideIn 0.3s ease;
}

@keyframes slideIn {
from {
opacity: 0;
transform: translateX(-10px);
}
to {
opacity: 1;
transform: translateX(0);
}
}

.url-info {
display: flex;
align-items: center;
gap: 15px;
flex-wrap: wrap;
flex: 2;
}

.url-flags {
display: flex;
gap: 5px;
}

.flag-icon {
width: 28px;
height: 28px;
border-radius: 6px;
object-fit: cover;
}

.url-text {
color: #E2E8F0;
font-size: 0.95rem;
word-break: break-all;
}

.url-actions {
display: flex;
gap: 10px;
}

.url-action-btn {
background: none;
border: none;
color: #64748B;
cursor: pointer;
padding: 8px;
border-radius: 8px;
transition: all 0.3s ease;
}

.url-action-btn:hover {
background: rgba(239, 68, 68, 0.1);
color: #EF4444;
}

/* Language Selection */
.language-section {
background: rgba(255, 255, 255, 0.02);
border: 1px solid rgba(255, 255, 255, 0.05);
border-radius: 24px;
padding: 30px;
margin-bottom: 30px;
}

.language-selector {
display: grid;
grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
gap: 15px;
margin-bottom: 20px;
}

.language-select {
position: relative;
}

.language-select i {
position: absolute;
left: 15px;
top: 50%;
transform: translateY(-50%);
color: #FBBF24;
z-index: 1;
}

.language-select select {
width: 100%;
padding: 15px 20px 15px 45px;
background: rgba(255, 255, 255, 0.02);
border: 2px solid rgba(255, 255, 255, 0.05);
border-radius: 12px;
color: white;
font-size: 0.95rem;
cursor: pointer;
appearance: none;
}

.language-select select option {
background: #1A2332;
color: white;
}

.language-select::after {
content: '\f078';
font-family: 'Font Awesome 6 Free';
font-weight: 900;
position: absolute;
right: 15px;
top: 50%;
transform: translateY(-50%);
color: #94A3B8;
pointer-events: none;
}

.country-select {
position: relative;
}

.country-select i {
position: absolute;
left: 15px;
top: 50%;
transform: translateY(-50%);
color: #34D399;
}

.country-select select {
width: 100%;
padding: 15px 20px 15px 45px;
background: rgba(255, 255, 255, 0.02);
border: 2px solid rgba(255, 255, 255, 0.05);
border-radius: 12px;
color: white;
font-size: 0.95rem;
cursor: pointer;
appearance: none;
}

/* X-default Option */
.xdefault-option {
margin: 20px 0;
padding: 20px;
background: rgba(251, 191, 36, 0.05);
border: 1px solid rgba(251, 191, 36, 0.1);
border-radius: 16px;
display: flex;
align-items: center;
gap: 20px;
flex-wrap: wrap;
}

.xdefault-checkbox {
display: flex;
align-items: center;
gap: 10px;
}

.xdefault-checkbox input[type="checkbox"] {
width: 20px;
height: 20px;
cursor: pointer;
accent-color: #FBBF24;
}

.xdefault-checkbox label {
color: #FBBF24;
font-weight: 500;
}

.xdefault-url {
flex: 2;
min-width: 300px;
position: relative;
}

.xdefault-url i {
position: absolute;
left: 15px;
top: 50%;
transform: translateY(-50%);
color: #FBBF24;
}

.xdefault-url input {
width: 100%;
padding: 12px 20px 12px 45px;
background: rgba(255, 255, 255, 0.02);
border: 1px solid rgba(251, 191, 36, 0.2);
border-radius: 10px;
color: white;
}

/* Options */
.options-section {
display: flex;
gap: 30px;
margin: 30px 0;
flex-wrap: wrap;
padding: 20px;
background: rgba(255, 255, 255, 0.02);
border-radius: 16px;
}

.option-item {
display: flex;
align-items: center;
gap: 10px;
color: #94A3B8;
}

.option-item input[type="radio"],
.option-item input[type="checkbox"] {
width: 18px;
height: 18px;
cursor: pointer;
accent-color: #60A5FA;
}

/* Generate Button */
.generate-btn {
width: 100%;
padding: 20px;
background: linear-gradient(135deg, #60A5FA, #34D399);
border: none;
border-radius: 20px;
color: white;
font-size: 1.3rem;
font-weight: 600;
cursor: pointer;
transition: all 0.3s ease;
display: flex;
align-items: center;
justify-content: center;
gap: 15px;
margin-top: 30px;
}

.generate-btn:hover {
transform: translateY(-3px);
box-shadow: 0 20px 30px -10px rgba(96, 165, 250, 0.5);
}

.generate-btn i {
font-size: 1.5rem;
}

/* Output Section */
.output-section {
background: rgba(255, 255, 255, 0.02);
border: 1px solid rgba(255, 255, 255, 0.05);
border-radius: 24px;
padding: 30px;
margin-top: 30px;
display: none;
}

.output-header {
display: flex;
justify-content: space-between;
align-items: center;
margin-bottom: 25px;
flex-wrap: wrap;
gap: 15px;
}

.output-header h2 {
display: flex;
align-items: center;
gap: 12px;
color: #34D399;
}

.output-actions {
display: flex;
gap: 12px;
}

.action-btn {
background: rgba(255, 255, 255, 0.03);
border: 1px solid rgba(255, 255, 255, 0.1);
color: #94A3B8;
padding: 12px 24px;
border-radius: 12px;
cursor: pointer;
transition: all 0.3s ease;
display: flex;
align-items: center;
gap: 8px;
font-size: 0.95rem;
}

.action-btn:hover {
background: rgba(52, 211, 153, 0.1);
border-color: #34D399;
color: #34D399;
}

/* Code Output */
.code-output {
background: #0B1120;
border-radius: 16px;
padding: 25px;
margin-bottom: 25px;
border: 1px solid rgba(255, 255, 255, 0.05);
position: relative;
}

.code-type-tabs {
display: flex;
gap: 10px;
margin-bottom: 20px;
border-bottom: 1px solid rgba(255, 255, 255, 0.05);
padding-bottom: 15px;
}

.code-tab {
padding: 10px 25px;
background: rgba(255, 255, 255, 0.02);
border: 1px solid rgba(255, 255, 255, 0.05);
border-radius: 30px;
color: #94A3B8;
cursor: pointer;
transition: all 0.3s ease;
font-size: 0.95rem;
}

.code-tab:hover {
background: rgba(96, 165, 250, 0.1);
border-color: #60A5FA;
color: #60A5FA;
}

.code-tab.active {
background: #60A5FA;
border-color: #60A5FA;
color: white;
}

.code-container {
position: relative;
}

pre {
background: rgba(255, 255, 255, 0.02);
padding: 25px;
border-radius: 12px;
overflow-x: auto;
color: #E2E8F0;
font-family: 'Fira Code', monospace;
font-size: 0.9rem;
line-height: 1.6;
border: 1px solid rgba(255, 255, 255, 0.05);
}

.copy-code-btn {
position: absolute;
top: 15px;
right: 15px;
background: rgba(255, 255, 255, 0.05);
border: 1px solid rgba(255, 255, 255, 0.1);
color: #94A3B8;
padding: 8px 16px;
border-radius: 8px;
cursor: pointer;
transition: all 0.3s ease;
display: flex;
align-items: center;
gap: 8px;
font-size: 0.9rem;
}

.copy-code-btn:hover {
background: #34D399;
border-color: #34D399;
color: white;
}

/* Preview Section */
.preview-section {
background: rgba(255, 255, 255, 0.02);
border-radius: 16px;
padding: 25px;
margin-top: 25px;
}

.preview-section h3 {
color: #94A3B8;
margin-bottom: 20px;
display: flex;
align-items: center;
gap: 10px;
}

.preview-grid {
display: grid;
grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
gap: 15px;
}

.preview-item {
background: rgba(255, 255, 255, 0.02);
border: 1px solid rgba(255, 255, 255, 0.05);
border-radius: 12px;
padding: 15px;
display: flex;
align-items: center;
gap: 12px;
}

.preview-flag {
width: 32px;
height: 32px;
border-radius: 8px;
}

.preview-info {
flex: 1;
}

.preview-lang {
font-weight: 600;
color: white;
margin-bottom: 4px;
}

.preview-url {
font-size: 0.8rem;
color: #60A5FA;
word-break: break-all;
}

/* Validation Warnings */
.validation-warnings {
background: rgba(245, 158, 11, 0.1);
border: 1px solid rgba(245, 158, 11, 0.2);
border-radius: 12px;
padding: 20px;
margin-top: 20px;
display: none;
}

.warning-title {
color: #F59E0B;
display: flex;
align-items: center;
gap: 10px;
margin-bottom: 10px;
font-weight: 600;
}

.warning-list {
list-style: none;
color: #FCD34D;
font-size: 0.95rem;
}

.warning-list li {
margin-bottom: 8px;
display: flex;
align-items: center;
gap: 10px;
}

/* Info Cards */
.info-grid {
display: grid;
grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
gap: 25px;
margin: 40px 0;
}

.info-card {
background: rgba(255, 255, 255, 0.02);
border: 1px solid rgba(255, 255, 255, 0.05);
border-radius: 20px;
padding: 25px;
}

.info-card h3 {
color: #60A5FA;
margin-bottom: 15px;
display: flex;
align-items: center;
gap: 10px;
}

.info-card p {
color: #94A3B8;
line-height: 1.6;
margin-bottom: 15px;
}

.info-card ul {
list-style: none;
}

.info-card ul li {
color: #CBD5E1;
margin-bottom: 10px;
display: flex;
align-items: center;
gap: 10px;
}

.info-card ul li i {
color: #34D399;
font-size: 0.9rem;
}

/* Footer */
.footer {
margin-top: 50px;
padding-top: 30px;
border-top: 1px solid rgba(255, 255, 255, 0.05);
}

.faq-grid {
display: grid;
grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
gap: 25px;
}

.faq-item {
background: rgba(255, 255, 255, 0.02);
padding: 25px;
border-radius: 16px;
}

.faq-item h4 {
color: #FBBF24;
margin-bottom: 10px;
display: flex;
align-items: center;
gap: 8px;
}

.faq-item p {
color: #94A3B8;
line-height: 1.6;
}

/* Loading */
.loading {
display: none;
text-align: center;
padding: 40px;
}

.spinner {
width: 50px;
height: 50px;
border: 4px solid rgba(96, 165, 250, 0.1);
border-top-color: #60A5FA;
border-radius: 50%;
animation: spin 1s linear infinite;
margin: 0 auto 20px;
}

@keyframes spin {
to { transform: rotate(360deg); }
}

/* Responsive */
@media (max-width: 768px) {
body { padding: 15px; }
.header h1 { font-size: 2.2rem; }
.main-card { padding: 25px; }
}
</style>
</head>
<body>
<div class="container">
<!-- Header -->
<div class="header">
<h1>
<i class="fas fa-globe"></i>
Hreflang Tag Generator
</h1>
<p>Generate hreflang tags for 100+ languages and countries. Perfect for international SEO and multilingual websites.</p>
<div class="language-badge">
<span class="badge"><i class="fas fa-language"></i> 100+ Languages</span>
<span class="badge"><i class="fas fa-map-marker-alt"></i> 200+ Countries</span>
<span class="badge"><i class="fas fa-code"></i> Multiple Formats</span>
<span class="badge"><i class="fas fa-check-circle"></i> W3C Valid</span>
</div>
</div>

<!-- Main Card -->
<div class="main-card">
<!-- URL Input Section -->
<div class="url-section">
<div class="section-title">
<i class="fas fa-link"></i>
<h2>Add Your URLs</h2>
</div>

<div class="url-input-group">
<div class="url-input-wrapper">
<i class="fas fa-globe"></i>
<input type="url" id="pageUrl" placeholder="Enter page URL (e.g., https://example.com/page)">
</div>
<button class="add-btn" id="addUrlBtn">
<i class="fas fa-plus"></i>
Add URL
</button>
</div>

<!-- URLs List -->
<div class="urls-list" id="urlsList">
<!-- URLs will be added here -->
</div>
</div>

<!-- Language Selection Section -->
<div class="language-section">
<div class="section-title">
<i class="fas fa-language"></i>
<h2>Select Languages & Regions</h2>
</div>

<div class="language-selector" id="languageSelector">
<!-- Language selects will be added here -->
</div>

<button class="add-btn" id="addLanguageBtn" style="width: auto; padding: 12px 30px;">
<i class="fas fa-plus-circle"></i>
Add Another Language
</button>

<!-- X-default Option -->
<div class="xdefault-option">
<div class="xdefault-checkbox">
<input type="checkbox" id="useXdefault">
<label for="useXdefault">Add x-default (default language page)</label>
</div>
<div class="xdefault-url">
<i class="fas fa-link"></i>
<input type="url" id="xdefaultUrl" placeholder="https://example.com/default-page">
</div>
</div>

<!-- Options -->
<div class="options-section">
<div class="option-item">
<input type="radio" name="outputFormat" id="formatHtml" value="html" checked>
<label for="formatHtml">HTML Link Tags</label>
</div>
<div class="option-item">
<input type="radio" name="outputFormat" id="formatHttp" value="http">
<label for="formatHttp">HTTP Headers</label>
</div>
<div class="option-item">
<input type="radio" name="outputFormat" id="formatXml" value="xml">
<label for="formatXml">XML Sitemap</label>
</div>
<div class="option-item">
<input type="checkbox" id="includeSelf" checked>
<label for="includeSelf">Include self-referencing hreflang</label>
</div>
</div>
</div>

<!-- Generate Button -->
<button class="generate-btn" id="generateBtn">
<i class="fas fa-magic"></i>
Generate Hreflang Tags
</button>

<!-- Loading -->
<div class="loading" id="loading">
<div class="spinner"></div>
<p>Generating hreflang tags...</p>
</div>

<!-- Output Section -->
<div class="output-section" id="outputSection">
<div class="output-header">
<h2>
<i class="fas fa-code"></i>
Generated Hreflang Tags
</h2>
<div class="output-actions">
<button class="action-btn" id="copyAllBtn">
<i class="fas fa-copy"></i>
Copy All
</button>
<button class="action-btn" id="downloadBtn">
<i class="fas fa-download"></i>
Download
</button>
</div>
</div>

<!-- Code Type Tabs -->
<div class="code-type-tabs">
<span class="code-tab active" data-type="html">HTML</span>
<span class="code-tab" data-type="http">HTTP Header</span>
<span class="code-tab" data-type="xml">XML Sitemap</span>
</div>

<!-- Code Output -->
<div class="code-output">
<div class="code-container">
<pre id="codeDisplay">&lt;link rel="alternate" hreflang="en-us" href="https://example.com/en-us/page" /&gt;
&lt;link rel="alternate" hreflang="en-gb" href="https://example.com/en-gb/page" /&gt;
&lt;link rel="alternate" hreflang="es-es" href="https://example.com/es-es/page" /&gt;
&lt;link rel="alternate" hreflang="fr-fr" href="https://example.com/fr-fr/page" /&gt;
&lt;link rel="alternate" hreflang="de-de" href="https://example.com/de-de/page" /&gt;
&lt;link rel="alternate" hreflang="x-default" href="https://example.com/page" /&gt;</pre>
<button class="copy-code-btn" id="copyCodeBtn">
<i class="fas fa-copy"></i>
Copy
</button>
</div>
</div>

<!-- Validation Warnings -->
<div class="validation-warnings" id="warnings">
<div class="warning-title">
<i class="fas fa-exclamation-triangle"></i>
Validation Warnings
</div>
<ul class="warning-list" id="warningList">
<li><i class="fas fa-times-circle"></i> No return links found for some URLs</li>
</ul>
</div>

<!-- Preview Section -->
<div class="preview-section">
<h3><i class="fas fa-eye"></i> Preview Your Hreflang Implementation</h3>
<div class="preview-grid" id="previewGrid">
<!-- Preview items will be added here -->
</div>
</div>
</div>
</div>

<!-- Information Cards -->
<div class="info-grid">
<div class="info-card">
<h3><i class="fas fa-check-circle"></i> What is Hreflang?</h3>
<p>Hreflang tags tell Google which language and regional URL to show in search results. Essential for multilingual websites to avoid duplicate content issues.</p>
<ul>
<li><i class="fas fa-arrow-right"></i> Prevents duplicate content penalties</li>
<li><i class="fas fa-arrow-right"></i> Shows right language to right users</li>
<li><i class="fas fa-arrow-right"></i> Improves user experience globally</li>
<li><i class="fas fa-arrow-right"></i> Required for international SEO</li>
</ul>
</div>

<div class="info-card">
<h3><i class="fas fa-language"></i> Supported Languages</h3>
<p>100+ languages including:</p>
<ul style="display: grid; grid-template-columns: repeat(2, 1fr);">
<li><i class="fas fa-check"></i> English (en)</li>
<li><i class="fas fa-check"></i> Spanish (es)</li>
<li><i class="fas fa-check"></i> French (fr)</li>
<li><i class="fas fa-check"></i> German (de)</li>
<li><i class="fas fa-check"></i> Hindi (hi)</li>
<li><i class="fas fa-check"></i> Chinese (zh)</li>
<li><i class="fas fa-check"></i> Arabic (ar)</li>
<li><i class="fas fa-check"></i> Russian (ru)</li>
<li><i class="fas fa-check"></i> Japanese (ja)</li>
<li><i class="fas fa-check"></i> Portuguese (pt)</li>
</ul>
</div>

<div class="info-card">
<h3><i class="fas fa-map-marked-alt"></i> Country Codes</h3>
<p>All ISO 3166-1 alpha-2 country codes supported:</p>
<ul style="display: grid; grid-template-columns: repeat(2, 1fr);">
<li><i class="fas fa-flag-usa"></i> US - United States</li>
<li><i class="fas fa-flag-gb"></i> GB - United Kingdom</li>
<li><i class="fas fa-flag-in"></i> IN - India</li>
<li><i class="fas fa-flag-ca"></i> CA - Canada</li>
<li><i class="fas fa-flag-au"></i> AU - Australia</li>
<li><i class="fas fa-flag-de"></i> DE - Germany</li>
</ul>
</div>
</div>

<!-- Footer FAQs -->
<div class="footer">
<h2 style="margin-bottom: 25px;"><i class="fas fa-question-circle"></i> Frequently Asked Questions</h2>
<div class="faq-grid">
<div class="faq-item">
<h4><i class="fas fa-question"></i> What is x-default?</h4>
<p>x-default is the default language version shown to users when no specific language matches their browser settings. Usually points to your main homepage or language selector page.</p>
</div>
<div class="faq-item">
<h4><i class="fas fa-question"></i> Where to place hreflang tags?</h4>
<p>Place hreflang tags in the &lt;head&gt; section of your HTML, in HTTP headers, or in XML sitemaps. All three methods are equally valid.</p>
</div>
<div class="faq-item">
<h4><i class="fas fa-question"></i> Do I need return links?</h4>
<p>Yes! If Page A links to Page B, Page B must link back to Page A. Google validates bidirectional links. Our generator ensures proper return links.</p>
</div>
<div class="faq-item">
<h4><i class="fas fa-question"></i> How many hreflang tags per page?</h4>
<p>Include all language variations of a page. If you have 5 language versions, each page should have 5 hreflang tags (including self-reference).</p>
</div>
<div class="faq-item">
<h4><i class="fas fa-question"></i> Can I use country codes without language?</h4>
<p>Yes, format "en-US" means English language for US. You can also use just language "en" for all English speakers regardless of country.</p>
</div>
<div class="faq-item">
<h4><i class="fas fa-question"></i> How to test hreflang implementation?</h4>
<p>Use Google Search Console's International Targeting report or our preview section above to verify your hreflang tags are correct.</p>
</div>
</div>
</div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
// DOM Elements
const addUrlBtn = document.getElementById('addUrlBtn');
const pageUrl = document.getElementById('pageUrl');
const urlsList = document.getElementById('urlsList');
const addLanguageBtn = document.getElementById('addLanguageBtn');
const languageSelector = document.getElementById('languageSelector');
const generateBtn = document.getElementById('generateBtn');
const loading = document.getElementById('loading');
const outputSection = document.getElementById('outputSection');
const codeDisplay = document.getElementById('codeDisplay');
const copyCodeBtn = document.getElementById('copyCodeBtn');
const copyAllBtn = document.getElementById('copyAllBtn');
const downloadBtn = document.getElementById('downloadBtn');
const previewGrid = document.getElementById('previewGrid');
const warnings = document.getElementById('warnings');
const warningList = document.getElementById('warningList');
const useXdefault = document.getElementById('useXdefault');
const xdefaultUrl = document.getElementById('xdefaultUrl');
const includeSelf = document.getElementById('includeSelf');

// Data stores
let urls = [
{ id: 1, url: 'https://example.com/en-us/page', languages: ['en-US'] },
{ id: 2, url: 'https://example.com/en-gb/page', languages: ['en-GB'] },
{ id: 3, url: 'https://example.com/es-es/page', languages: ['es-ES'] }
];

let languages = [
{ lang: 'en', country: 'US' },
{ lang: 'en', country: 'GB' },
{ lang: 'es', country: 'ES' }
];

// Language data
const languagesData = {
'en': 'English', 'es': 'Spanish', 'fr': 'French', 'de': 'German',
'it': 'Italian', 'pt': 'Portuguese', 'ru': 'Russian', 'zh': 'Chinese',
'ja': 'Japanese', 'ko': 'Korean', 'ar': 'Arabic', 'hi': 'Hindi',
'bn': 'Bengali', 'pa': 'Punjabi', 'te': 'Telugu', 'ta': 'Tamil',
'ur': 'Urdu', 'tr': 'Turkish', 'nl': 'Dutch', 'pl': 'Polish',
'vi': 'Vietnamese', 'th': 'Thai', 'id': 'Indonesian', 'ms': 'Malay',
'fa': 'Persian', 'sw': 'Swahili', 'tl': 'Tagalog', 'ro': 'Romanian',
'hu': 'Hungarian', 'cs': 'Czech', 'el': 'Greek', 'he': 'Hebrew',
'sv': 'Swedish', 'no': 'Norwegian', 'da': 'Danish', 'fi': 'Finnish',
'sk': 'Slovak', 'bg': 'Bulgarian', 'sr': 'Serbian', 'hr': 'Croatian',
'uk': 'Ukrainian', 'lt': 'Lithuanian', 'lv': 'Latvian', 'et': 'Estonian',
'sl': 'Slovenian', 'mt': 'Maltese', 'ga': 'Irish', 'cy': 'Welsh',
'af': 'Afrikaans', 'zu': 'Zulu', 'xh': 'Xhosa', 'st': 'Sesotho',
'tn': 'Setswana', 'ss': 'Swati', 've': 'Venda', 'nr': 'Ndebele'
};

const countriesData = {
'US': 'United States', 'GB': 'United Kingdom', 'IN': 'India',
'CA': 'Canada', 'AU': 'Australia', 'DE': 'Germany',
'FR': 'France', 'ES': 'Spain', 'IT': 'Italy',
'PT': 'Portugal', 'BR': 'Brazil', 'MX': 'Mexico',
'AR': 'Argentina', 'CO': 'Colombia', 'CL': 'Chile',
'PE': 'Peru', 'VE': 'Venezuela', 'EC': 'Ecuador',
'JP': 'Japan', 'CN': 'China', 'KR': 'South Korea',
'RU': 'Russia', 'ZA': 'South Africa', 'NG': 'Nigeria',
'EG': 'Egypt', 'SA': 'Saudi Arabia', 'AE': 'UAE',
'IL': 'Israel', 'TR': 'Turkey', 'PK': 'Pakistan',
'BD': 'Bangladesh', 'LK': 'Sri Lanka', 'NP': 'Nepal',
'MY': 'Malaysia', 'SG': 'Singapore', 'PH': 'Philippines',
'VN': 'Vietnam', 'TH': 'Thailand', 'ID': 'Indonesia'
};

// Flag emoji mapping
const flagEmoji = {
'US': '🇺🇸', 'GB': '🇬🇧', 'IN': '🇮🇳', 'CA': '🇨🇦',
'AU': '🇦🇺', 'DE': '🇩🇪', 'FR': '🇫🇷', 'ES': '🇪🇸',
'IT': '🇮🇹', 'PT': '🇵🇹', 'BR': '🇧🇷', 'MX': '🇲🇽',
'JP': '🇯🇵', 'CN': '🇨🇳', 'KR': '🇰🇷', 'RU': '🇷🇺',
'SA': '🇸🇦', 'AE': '🇦🇪', 'IL': '🇮🇱', 'TR': '🇹🇷',
'ZA': '🇿🇦', 'NG': '🇳🇬', 'EG': '🇪🇬', 'PK': '🇵🇰',
'BD': '🇧🇩', 'LK': '🇱🇰', 'NP': '🇳🇵', 'MY': '🇲🇾',
'SG': '🇸🇬', 'PH': '🇵🇭', 'VN': '🇻🇳', 'TH': '🇹🇭',
'ID': '🇮🇩'
};

// Render URLs list
function renderUrls() {
if (urls.length === 0) {
urlsList.innerHTML = '<p style="color: #64748B; text-align: center; padding: 20px;">No URLs added yet. Add your first URL above.</p>';
return;
}

let html = '';
urls.forEach(url => {
const langFlags = url.languages.map(l => {
const country = l.split('-')[1];
return flagEmoji[country] || '🌐';
}).join(' ');

html += `
<div class="url-item">
<div class="url-info">
<div class="url-flags">
${langFlags}
</div>
<span class="url-text">${url.url}</span>
</div>
<div class="url-actions">
<button class="url-action-btn" onclick="removeUrl(${url.id})">
<i class="fas fa-trash"></i>
</button>
</div>
</div>
`;
});
urlsList.innerHTML = html;
}

// Remove URL
window.removeUrl = function(id) {
urls = urls.filter(u => u.id !== id);
renderUrls();
};

// Render language selectors
function renderLanguageSelectors() {
let html = '';
languages.forEach((lang, index) => {
html += `
<div class="language-selector" style="grid-column: 1/-1; display: grid; grid-template-columns: 1fr 1fr auto; gap: 15px; margin-bottom: 15px;">
<div class="language-select">
<i class="fas fa-language"></i>
<select class="lang-select" data-index="${index}">
${Object.entries(languagesData).map(([code, name]) =>
`<option value="${code}" ${lang.lang === code ? 'selected' : ''}>${name} (${code})</option>`
).join('')}
</select>
</div>
<div class="country-select">
<i class="fas fa-map-marker-alt"></i>
<select class="country-select" data-index="${index}">
${Object.entries(countriesData).map(([code, name]) =>
`<option value="${code}" ${lang.country === code ? 'selected' : ''}>${flagEmoji[code] || '🌐'} ${name} (${code})</option>`
).join('')}
</select>
</div>
<button class="add-btn" onclick="removeLanguage(${index})" style="min-width: 50px; background: rgba(239, 68, 68, 0.1); color: #EF4444;">
<i class="fas fa-times"></i>
</button>
</div>
`;
});
languageSelector.innerHTML = html;

// Add event listeners
document.querySelectorAll('.lang-select').forEach(select => {
select.addEventListener('change', function() {
const index = this.dataset.index;
languages[index].lang = this.value;
});
});

document.querySelectorAll('.country-select').forEach(select => {
select.addEventListener('change', function() {
const index = this.dataset.index;
languages[index].country = this.value;
});
});
}

// Remove language
window.removeLanguage = function(index) {
languages.splice(index, 1);
renderLanguageSelectors();
};

// Add URL
addUrlBtn.addEventListener('click', function() {
const url = pageUrl.value.trim();
if (!url) {
alert('Please enter a URL');
return;
}

// Validate URL
try {
new URL(url);
} catch {
alert('Please enter a valid URL (include http:// or https://)');
return;
}

urls.push({
id: Date.now(),
url: url,
languages: languages.map(l => `${l.lang}-${l.country}`)
});

pageUrl.value = '';
renderUrls();
});

// Add language
addLanguageBtn.addEventListener('click', function() {
languages.push({ lang: 'en', country: 'US' });
renderLanguageSelectors();
});

// Generate hreflang tags
generateBtn.addEventListener('click', function() {
if (urls.length === 0) {
alert('Please add at least one URL');
return;
}

// Show loading
loading.style.display = 'block';
outputSection.style.display = 'none';

setTimeout(() => {
try {
const format = document.querySelector('input[name="outputFormat"]:checked').value;
const includeSelfRef = includeSelf.checked;
const useXdefaultChecked = useXdefault.checked;
const xdefaultUrlValue = xdefaultUrl.value;

// Generate tags based on format
let tags = [];
let previewItems = [];

urls.forEach(url => {
url.languages.forEach(langCode => {
const [lang, country] = langCode.split('-');
const tag = {
lang: lang,
country: country,
url: url.url,
hreflang: `${lang}-${country}`
};
tags.push(tag);

// Add to preview
previewItems.push({
flag: flagEmoji[country] || '🌐',
lang: `${languagesData[lang] || lang} (${lang})`,
country: countriesData[country] || country,
url: url.url
});
});
});

// Add x-default if checked
if (useXdefaultChecked && xdefaultUrlValue) {
tags.push({
hreflang: 'x-default',
url: xdefaultUrlValue
});
}

// Generate code based on format
let code = '';
if (format === 'html') {
code = tags.map(t =>
t.hreflang === 'x-default'
? `<link rel="alternate" hreflang="x-default" href="${t.url}" />`
: `<link rel="alternate" hreflang="${t.hreflang}" href="${t.url}" />`
).join('\n');
} else if (format === 'http') {
code = tags.map(t =>
`Link: <${t.url}>; rel="alternate"; hreflang="${t.hreflang}"`
).join('\n');
} else {
code = `<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"\n xmlns:xhtml="http://www.w3.org/1999/xhtml">\n`;
urls.forEach(url => {
code += ` <url>\n <loc>${url.url}</loc>\n`;
url.languages.forEach(langCode => {
code += ` <xhtml:link rel="alternate" hreflang="${langCode}" href="${url.url}" />\n`;
});
code += ` </url>\n`;
});
code += `</urlset>`;
}

// Update display
codeDisplay.textContent = code;

// Update preview
let previewHtml = '';
previewItems.slice(0, 6).forEach(item => {
previewHtml += `
<div class="preview-item">
<span style="font-size: 2rem;">${item.flag}</span>
<div class="preview-info">
<div class="preview-lang">${item.lang} - ${item.country}</div>
<div class="preview-url">${item.url.substring(0, 40)}...</div>
</div>
</div>
`;
});
previewGrid.innerHTML = previewHtml;

// Check for warnings
const hasWarnings = tags.length < urls.length * 2;
if (hasWarnings) {
warnings.style.display = 'block';
warningList.innerHTML = `
<li><i class="fas fa-exclamation-circle"></i> Some URLs don't have return links. Ensure bidirectional linking.</li>
<li><i class="fas fa-exclamation-circle"></i> ${urls.length} URLs with ${tags.length} language variations.</li>
`;
} else {
warnings.style.display = 'none';
}

// Show output
loading.style.display = 'none';
outputSection.style.display = 'block';
} catch (err) {
loading.style.display = 'none';
alert('Error generating tags. Please check your inputs.');
}
}, 1000);
});

// Copy code
copyCodeBtn.addEventListener('click', function() {
const code = codeDisplay.textContent;
navigator.clipboard.writeText(code).then(() => {
const btn = this;
const original = btn.innerHTML;
btn.innerHTML = '<i class="fas fa-check"></i> Copied!';
setTimeout(() => btn.innerHTML = original, 2000);
});
});

// Copy all
copyAllBtn.addEventListener('click', function() {
const code = codeDisplay.textContent;
navigator.clipboard.writeText(code).then(() => {
const btn = this;
const original = btn.innerHTML;
btn.innerHTML = '<i class="fas fa-check"></i> Copied!';
setTimeout(() => btn.innerHTML = original, 2000);
});
});

// Download
downloadBtn.addEventListener('click', function() {
const code = codeDisplay.textContent;
const format = document.querySelector('input[name="outputFormat"]:checked').value;
const extension = format === 'html' ? 'html' : format === 'xml' ? 'xml' : 'txt';
const blob = new Blob([code], { type: 'text/plain' });
const url = window.URL.createObjectURL(blob);
const a = document.createElement('a');
a.href = url;
a.download = `hreflang-tags.${extension}`;
a.click();
});

// Format tabs
document.querySelectorAll('.code-tab').forEach(tab => {
tab.addEventListener('click', function() {
document.querySelectorAll('.code-tab').forEach(t => t.classList.remove('active'));
this.classList.add('active');

// Trigger generate with new format
generateBtn.click();
});
});

// Initial render
renderUrls();
renderLanguageSelectors();
});
</script>
</body>
</html>